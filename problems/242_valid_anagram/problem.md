# [242. Valid Anagram](https://leetcode.com/problems/valid-anagram/)

## Java

### v1: sort and compare

### v2: two set

Use HashSet to track char occurrences, slower than v1.

### v3: array as set

Use integer array to track occurrences. Since the input is lowercase english characters.
